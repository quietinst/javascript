# Task 01 — Slug Generator

## Objective

Create a function that converts a title into a URL-friendly slug.

The slug should contain lowercase words separated by hyphens.

### Concepts

- Functions
- Strings
- String methods
- `toLowerCase()`
- `split()`
- `join()`
