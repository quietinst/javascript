# Task 01 — Category List

## Objective

Count and log the categories in a nested list, along with each category's title and item count, using DOM properties and methods.

### Concepts

- DOM traversal
- `querySelectorAll()`
- `forEach()`
- `children` / `childElementCount`
