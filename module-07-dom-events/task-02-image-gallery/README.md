# Task 02 — Image Gallery

## Objective

Build an image gallery from an array of objects, generating and inserting all elements into the DOM in a single operation.

### Concepts

- DOM element creation
- `map()`
- `insertAdjacentHTML()` / `createElement()`
- Template literals
- CSS flexbox
