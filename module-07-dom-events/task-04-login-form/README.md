# Task 04 — Login Form

## Objective

Handle form submission via JavaScript, validate that all fields are filled, collect the form data into an object, and reset the form.

### Concepts

- `submit` event
- `preventDefault()`
- `form.elements`
- `trim()`
- `reset()`
