# Task 01 — Image Gallery

## Objective

Refactor the previous manual gallery (event delegation + a hand-built modal) into a gallery powered by the SimpleLightbox npm package, which handles clicks, opening/closing the modal, and keyboard navigation on its own.

### Concepts

- npm packages
- ECMAScript Modules (`import`)
- Third-party library integration (SimpleLightbox)
- Library configuration options
