# Task 02 — Feedback Form

## Objective

Create a feedback form that saves its field values to localStorage as the user types, restores them on page load, and validates and clears them on submit.

### Concepts

- `localStorage`
- `JSON.stringify()` / `JSON.parse()`
- Event delegation (`input` event)
- Form validation
- `trim()`
